# Автоматический расчет для обеих печей со средним значением

## Дата: 2 февраля 2026

## Изменения

### 1. Убран выбор печей
- ❌ Удален компонент `KilnSelector` из первой вкладки
- ✅ Автоматически рассчитываем для обеих печей (большая + малая)
- ✅ Пользователь не выбирает - программа считает сама

### 2. Новая логика результатов

#### Если помещается в обе печи:
```
Результат = Среднее арифметическое
Изделий в печи = (БольшаяПечь + МалаяПечь) / 2 (округлено)
Площадь загрузки = (БольшаяПечь + МалаяПечь) / 2
```

**Пример:**
- Большая печь: 100 шт, 6.0 м²
- Малая печь: 120 шт, 7.2 м²
- **Результат: 110 шт, 6.6 м²**

#### Если помещается только в одну печь:
```
Результат = Данные этой печи
```

**Примеры:**
- Столешница 100×60 → только малая печь → результат малой печи
- Мелкое изделие → обе печи → среднее значение

### 3. Новый компактный дизайн результатов

**Было:**
- Два столбца с результатами для каждой печи
- Названия печей
- Детальная информация для каждой

**Стало:**
- Одно компактное отображение
- Только итоговые числа:
  - 📦 Изделий в печи: **110 шт**
  - 📐 Площадь загрузки: **6.60 м²**
- Информация о методе и печах ниже

### 4. Структура результатов

```
┌─────────────────────────────────────┐
│  Результаты расчета                 │
│                                     │
│  ┌─────────────┬─────────────────┐ │
│  │ 📦          │ 📐              │ │
│  │ Изделий     │ Площадь         │ │
│  │ 110 шт      │ 6.60 м²         │ │
│  └─────────────┴─────────────────┘ │
│                                     │
│  Метод: Среднее по обеим печам     │
│  Большая: 100 шт, Малая: 120 шт    │
└─────────────────────────────────────┘
```

## Логика расчета

### Код расчета среднего:

```typescript
if (bigResult && smallResult) {
  // Обе печи - среднее арифметическое
  displayPieces = Math.round(
    (bigResult.optimalLoading.totalPieces + 
     smallResult.optimalLoading.totalPieces) / 2
  );
  displayArea = 
    (bigResult.optimalLoading.totalArea + 
     smallResult.optimalLoading.totalArea) / 2;
  displayInfo = `Большая печь: ${bigResult.optimalLoading.totalPieces} шт, 
                 Малая печь: ${smallResult.optimalLoading.totalPieces} шт`;
}
```

### Автоматический выбор:

```typescript
// Всегда считаем для обеих печей
const [selectedKilns] = useState<KilnType[]>(['big', 'small']);
```

### Обработка ограничений:

```typescript
// Столешницы в большой печи
if (productType === 'countertop' && kilnType === 'big') {
  return null; // Не считаем для большой печи
}

// Тогда результат = только малая печь
```

## Преимущества

### Для пользователя:
✅ Не нужно выбирать печь  
✅ Сразу видно итоговый результат  
✅ Простой интерфейс  
✅ Меньше кликов  
✅ Автоматическая оптимизация  

### Для производства:
✅ Универсальный расчет  
✅ Среднее значение для планирования  
✅ Видно детали (большая/малая)  
✅ Учитываются ограничения автоматически  

## Примеры работы

### Пример 1: Плитка 30×40×2 см
**Помещается в обе печи:**
- Большая: 150 шт, 9.0 м²
- Малая: 180 шт, 10.8 м²
- **Показываем: 165 шт, 9.9 м²**
- Метод: "Среднее по обеим печам"

### Пример 2: Столешница 100×60×3 см
**Помещается только в малую:**
- Большая: ❌ (столешницы запрещены)
- Малая: 1 шт, 0.6 м²
- **Показываем: 1 шт, 0.6 м²**
- Метод: "Плашмя (лицом вверх)"
- Информация: "Малая печь (95×150 см)"

### Пример 3: Большое изделие 80×120×3 см
**Помещается только в малую:**
- Большая: ❌ (не помещается)
- Малая: 1 шт, 0.96 м²
- **Показываем: 1 шт, 0.96 м²**
- Информация: "Малая печь (95×150 см)"

## CSS изменения

### Новые стили:
```css
.result-stats-compact {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1rem;
}

.stat-item-large {
  padding: 1.25rem;
  background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
}

.stat-value-large {
  font-size: 2rem;
  font-weight: 700;
  color: var(--primary-color);
}
```

### Крупные цифры:
- Количество и площадь: **2rem** (32px)
- Яркий акцент
- Легко читаются

## Файлы изменены

1. **`src/App.tsx`**
   - Удален import `KilnSelector`
   - `selectedKilns` зафиксирован: `['big', 'small']`
   - Убран компонент выбора печей из JSX

2. **`src/components/MultiKilnResults.tsx`**
   - Полностью переписан
   - Логика среднего значения
   - Компактное отображение
   - Одно значение вместо двух столбцов

3. **`src/App.css`**
   - Добавлены стили для `.result-stats-compact`
   - Крупные значения `.stat-value-large`
   - Градиентный фон для карточек

## Build статус

✅ **Build успешен**  
✅ **TypeScript**: Без ошибок  
✅ **Bundle**: 171.15 kB (53.44 kB gzipped)  
✅ **CSS**: 16.59 kB (3.39 kB gzipped)

---

**Обновите страницу:** http://localhost:5177/

Теперь:
- Нет выбора печей
- Автоматический расчет
- Среднее значение, если помещается в обе
- Простой и понятный результат!
