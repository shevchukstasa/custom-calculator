# ✅ ГОТОВО: Фиксированная ссылка для калькулятора

## Дата: 2 февраля 2026

---

## 🎯 Ваша постоянная ссылка:

```
http://localhost:5173
```

**Сохраните эту ссылку в закладки!**

Она будет работать всегда, когда сервер запущен.

---

## Что было сделано:

### 1. Настроен фиксированный порт ✅

**Файл:** `vite.config.ts`
```typescript
server: {
  port: 5173,  // Всегда пытается использовать этот порт
  strictPort: false,
  host: true,
  open: false,
}
```

### 2. Создан скрипт автозапуска ✅

**Файл:** `start.sh`

Скрипт автоматически:
- Останавливает все старые серверы на портах 5173-5178
- Запускает новый сервер на порту 5173

### 3. Сервер запущен ✅

```
✅ VITE v5.4.21  ready in 221 ms

➜  Local:   http://localhost:5173/
➜  Network: http://192.168.18.146:5173/
➜  Network: http://10.2.0.2:5173/
```

---

## 📖 Как использовать:

### Вариант 1: Автоматический запуск (рекомендуется)

**В терминале:**
```bash
cd /Users/ss/kiln-calculator
./start.sh
```

**Что произойдет:**
- Остановятся все старые серверы
- Запустится новый сервер на порту 5173
- Откроется: http://localhost:5173

---

### Вариант 2: Вручную через npm

**В терминале:**
```bash
cd /Users/ss/kiln-calculator
npm run dev
```

**Если порт 5173 занят:**
```bash
# Остановите старый сервер:
lsof -ti:5173 | xargs kill -9

# Запустите снова:
npm run dev
```

---

## 🔍 Проверка статуса сервера:

**Запущен ли сервер?**
```bash
lsof -i:5173
```

**Если показывает процесс** — сервер работает ✅  
**Если ничего** — сервер не запущен ❌

---

## ❓ Почему раньше порты менялись?

**Проблема:**
- Вы запускали `npm run dev` несколько раз
- Каждый раз создавался новый сервер
- Старые серверы не останавливались
- Новый сервер искал свободный порт: 5173 → 5174 → 5175 → ...

**Решение:**
- Скрипт `start.sh` автоматически останавливает старые серверы
- Vite настроен на порт 5173 по умолчанию
- Теперь всегда один сервер на одном порту!

---

## 🚀 Быстрый старт (прямо сейчас):

**Сервер уже запущен!**

Откройте в браузере:
```
http://localhost:5173
```

---

## 📁 Файлы:

1. **`vite.config.ts`** — Конфигурация с фиксированным портом
2. **`start.sh`** — Скрипт автозапуска
3. **`START_HERE.md`** — Краткая инструкция
4. **`FIXED_PORT_SETUP.md`** (этот файл) — Полная документация

---

## 🎨 Дополнительно: Доступ с других устройств

**Ваш компьютер виден в локальной сети:**
```
http://192.168.18.146:5173  (WiFi)
http://10.2.0.2:5173        (Другая сеть)
```

**Можно открыть калькулятор:**
- С телефона
- С планшета
- С другого компьютера в той же сети

---

## ⚙️ Технические детали:

### Порт 5173
- Стандартный порт для Vite
- Легко запомнить
- Не конфликтует с другими сервисами

### strictPort: false
- Если 5173 занят — пробует 5174, 5175...
- Но `start.sh` очищает порт, так что это не нужно

### host: true
- Сервер слушает на всех сетевых интерфейсах
- Доступен не только с localhost, но и из локальной сети

---

## 🛠️ Устранение проблем:

### Проблема: "Port 5173 is already in use"

**Решение 1:** Используйте скрипт
```bash
./start.sh
```

**Решение 2:** Остановите вручную
```bash
lsof -ti:5173 | xargs kill -9
npm run dev
```

---

### Проблема: Страница не открывается

**Проверьте:**
```bash
lsof -i:5173
```

**Если ничего** → запустите сервер:
```bash
./start.sh
```

---

### Проблема: Изменения не отображаются

**Обновите страницу:**
- Cmd + R (Mac)
- Ctrl + R (Windows/Linux)
- Или жесткое обновление: Cmd + Shift + R

---

## 📊 Статистика:

**Проблема решена:**
- ❌ Было: 5 разных портов (5173, 5174, 5175, 5177, 5178)
- ✅ Стало: 1 постоянный порт (5173)

**Удобство:**
- ❌ Было: Нужно каждый раз смотреть, какой порт
- ✅ Стало: Одна постоянная ссылка в закладках

---

## 🎉 Итог:

✅ **Фиксированный порт:** 5173  
✅ **Скрипт автозапуска:** `start.sh`  
✅ **Сервер запущен:** http://localhost:5173  
✅ **Документация:** 3 файла (START_HERE.md, FIXED_PORT_SETUP.md, vite.config.ts)

---

## 💡 Совет:

**Добавьте в закладки браузера:**
```
Название: Калькулятор печи
URL: http://localhost:5173
```

Теперь всегда можно открыть одним кликом!

---

**Ваш сервер работает! Откройте:** http://localhost:5173 🎉
