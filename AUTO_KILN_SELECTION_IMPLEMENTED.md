# Реализация: Умный автовыбор печи для расчета стоимости

## Дата: 3 февраля 2026

## Проблема

Ранее значение печи для расчета стоимости было статически установлено как `'big'`. Это не учитывало:
- Помещается ли изделие в обе печи
- Разницу в количестве изделий между печами
- Оптимальный выбор для расчета стоимости

## Решение

### 1. Функция `getDefaultCostKilnType`

**Файл:** `src/App.tsx` (после строки 70)

**Логика выбора:**

```typescript
const getDefaultCostKilnType = (
  results: Record<KilnType, CalculationResult | null>
): KilnType | 'average' => {
  const bigResult = results.big;
  const smallResult = results.small;
  
  // Случай 1: Ни одна печь не подошла
  if (!bigResult && !smallResult) {
    return 'big'; // Дефолт
  }
  
  // Случай 2: Только одна печь подошла
  if (bigResult && !smallResult) return 'big';
  if (!bigResult && smallResult) return 'small';
  
  // Случай 3: Обе печи подошли
  const bigPieces = bigResult!.optimalLoading.totalPieces;
  const smallPieces = smallResult!.optimalLoading.totalPieces;
  const maxPieces = Math.max(bigPieces, smallPieces);
  const minPieces = Math.min(bigPieces, smallPieces);
  
  // Вычисляем разницу в процентах
  const differencePercent = ((maxPieces - minPieces) / maxPieces) * 100;
  
  // Если разница <= 20%, показываем среднее
  if (differencePercent <= 20) {
    return 'average';
  }
  
  // Если разница > 20%, показываем печь с большей загрузкой
  return bigPieces > smallPieces ? 'big' : 'small';
};
```

### 2. Обновление `handleCalculate`

**Добавлена строка:** автоматический выбор печи после расчета загрузки

```typescript
const handleCalculate = (product: ProductDimensions) => {
  // ... расчет для печей ...
  
  setKilnResults(newResults);
  
  // Определяем лучшую печь
  const { kilnType: best } = getBestResult(newResults, selectedKilns);
  setBestKilnType(best);
  
  // НОВОЕ: Автоматически выбираем печь для расчета стоимости
  const defaultKiln = getDefaultCostKilnType(newResults);
  setCostKilnType(defaultKiln);
  
  // ... остальной код ...
};
```

### 3. Реализация расчета среднего значения

**Файл:** `src/App.tsx` - функция `calculateCostResult`

**Новая логика:**

```typescript
if (targetKilnType === 'average') {
  // Получаем результаты обеих печей
  const bigResult = kilnResults.big;
  const smallResult = kilnResults.small;
  
  if (!bigResult || !smallResult) {
    alert('Для расчета среднего нужны результаты по обеим печам');
    return;
  }
  
  // Рассчитываем стоимость для каждой печи
  const bigCost = calculateCost(...);
  const smallCost = calculateCost(...);
  
  // Создаем усредненный результат
  const averageResult: CostCalculationResult = {
    mode: bigCost.mode,
    product: bigCost.product,
    kiln: {
      ...bigCost.kiln,
      name: 'Среднее (обе печи)'
    },
    kilnLoading: {
      totalPieces: Math.round((big + small) / 2),
      totalArea: (big + small) / 2,
      levels: Math.round((big + small) / 2),
    },
    indonesia: {
      pricePerSqM: (big + small) / 2,
      pricePerPcs: (big + small) / 2,
      margin: (big + small) / 2,
      marginPercent: (big + small) / 2,
    },
    abroad: {
      pricePerSqM: (big + small) / 2,
      pricePerPcs: (big + small) / 2,
      margin: (big + small) / 2,
      marginPercent: (big + small) / 2,
    },
  };
  
  setCostResult(averageResult);
}
```

### 4. Обновление условного рендеринга

**Компонент `StoneCostInput`:**

```typescript
{(costKilnType === 'average' 
  ? (kilnResults.big || kilnResults.small) 
  : kilnResults[costKilnType as KilnType]
) && (
  <StoneCostInput
    productArea={
      costKilnType === 'average'
        ? ((kilnResults.big!.product.length * kilnResults.big!.product.width) / 10000)
        : ((kilnResults[costKilnType as KilnType]!.product.length *
           kilnResults[costKilnType as KilnType]!.product.width) / 10000)
    }
    // ... props
  />
)}
```

**Кнопка "Рассчитать стоимость":**

```typescript
<button
  onClick={() => {
    const currentProduct = costKilnType === 'average'
      ? (kilnResults.big || kilnResults.small)?.product
      : kilnResults[costKilnType as KilnType]?.product;
    
    const currentResult = costKilnType === 'average'
      ? (kilnResults.big || kilnResults.small)
      : kilnResults[costKilnType as KilnType];
    
    if (currentProduct && currentResult) {
      calculateCostResult(currentProduct, currentResult);
    }
  }}
>
  Рассчитать стоимость
</button>
```

**Hard mode параметры:**

```typescript
{costResult && mode === 'hard' && (
  <CostParametersComponent
    onChange={(updates) => {
      setCustomParams({ ...customParams, ...updates });
      
      const currentProduct = costKilnType === 'average'
        ? (kilnResults.big || kilnResults.small)?.product
        : kilnResults[costKilnType as KilnType]?.product;
      // ... пересчет
    }}
  />
)}
```

## Примеры работы

### Пример 1: Плитка 10×10 см (разница ≤ 20%)

**Входные данные:**
- Длина: 10 см
- Ширина: 10 см
- Толщина: 1 см

**Результаты расчета:**
- Большая печь: 892 шт
- Малая печь: 800 шт

**Расчет:**
```
maxPieces = 892
minPieces = 800
difference = (892 - 800) / 892 * 100 = 10.3%
```

**Автовыбор:** 10.3% ≤ 20% → **"Среднее по печам"**

**Результат расчета стоимости:**
- Изделий в печи: 846 шт (среднее)
- Цена за 1 м²: усредненная между печами
- Цена за 1 шт: усредненная между печами

---

### Пример 2: Столешница 60×40 см (только одна печь)

**Входные данные:**
- Длина: 60 см
- Ширина: 40 см
- Толщина: 2 см
- Тип: countertop

**Результаты расчета:**
- Большая печь: null (countertop не помещается в большую печь)
- Малая печь: 12 шт

**Автовыбор:** Только одна печь → **"Малая печь"**

**Результат расчета стоимости:**
- Изделий в печи: 12 шт
- Расчет для малой печи

---

### Пример 3: Плитка 5×5 см (разница > 20%)

**Входные данные:**
- Длина: 5 см
- Ширина: 5 см
- Толщина: 0.8 см

**Результаты расчета:**
- Большая печь: 3500 шт
- Малая печь: 1200 шт

**Расчет:**
```
maxPieces = 3500
minPieces = 1200
difference = (3500 - 1200) / 3500 * 100 = 65.7%
```

**Автовыбор:** 65.7% > 20% → **"Большая печь"**

**Результат расчета стоимости:**
- Изделий в печи: 3500 шт
- Расчет для большой печи

---

### Пример 4: Плитка 15×15 см (точно на границе 20%)

**Входные данные:**
- Длина: 15 см
- Ширина: 15 см
- Толщина: 1 см

**Результаты расчета:**
- Большая печь: 500 шт
- Малая печь: 400 шт

**Расчет:**
```
maxPieces = 500
minPieces = 400
difference = (500 - 400) / 500 * 100 = 20%
```

**Автовыбор:** 20% ≤ 20% → **"Среднее по печам"**

## Логика в диаграмме

```
┌─────────────────────────┐
│  Расчет загрузки печей  │
└────────────┬────────────┘
             │
             ▼
    ┌────────────────────┐
    │ Проверить          │
    │ результаты         │
    └────────┬───────────┘
             │
        ┌────┴────────────────────┐
        │                         │
        ▼                         ▼
┌──────────────┐         ┌──────────────┐
│ Обе печи: ✗  │         │ Обе печи: ✓  │
└──────┬───────┘         └──────┬───────┘
       │                        │
       ▼                        ▼
┌──────────────┐         ┌──────────────┐
│ Только big   │         │ Рассчитать   │
│ → big        │         │ разницу %    │
└──────────────┘         └──────┬───────┘
┌──────────────┐                │
│ Только small │         ┌──────┴───────┐
│ → small      │         │              │
└──────────────┘         ▼              ▼
┌──────────────┐    ┌─────────┐   ┌─────────┐
│ Ни одной     │    │ ≤ 20%   │   │ > 20%   │
│ → big (def)  │    │ average │   │ max     │
└──────────────┘    └─────────┘   └─────────┘
```

## Изменения в файлах

1. **src/App.tsx**
   - Добавлена функция `getDefaultCostKilnType` (строка 72-105)
   - Обновлена функция `handleCalculate` (строка 180)
   - Полностью переписана функция `calculateCostResult` (строка 200-299)
   - Обновлен условный рендеринг для `StoneCostInput` (строка 419-445)
   - Обновлен обработчик кнопки "Рассчитать стоимость" (строка 447-461)
   - Обновлен обработчик для hard mode параметров (строка 463-477)

## Результат сборки

```
✅ TypeScript compilation: OK
✅ Vite build: OK
✅ CSS: 17.06 kB (3.07 kB gzipped)
✅ JS:  177.12 kB (54.87 kB gzipped)
```

## Поведение системы

### Автоматический выбор

После каждого расчета загрузки печи система:
1. Анализирует результаты для обеих печей
2. Применяет логику умного выбора
3. Автоматически устанавливает значение селектора печи

### Ручное переключение

Пользователь всегда может:
1. Вручную выбрать другую печь из селектора
2. Переключиться на "Среднее по печам"
3. Пересчитать стоимость для выбранной печи

### Расчет среднего

Когда выбрано "Среднее по печам":
1. Проверяется наличие результатов для обеих печей
2. Рассчитывается стоимость для каждой печи отдельно
3. Усредняются все показатели (цены, маржа, количество)
4. Показывается результат с названием "Среднее (обе печи)"

## Преимущества

1. **Умный автовыбор** - система выбирает оптимальную печь автоматически
2. **Гибкость** - пользователь может вручную изменить выбор
3. **Точность** - расчет среднего основан на реальных данных обеих печей
4. **Наглядность** - четко видно разницу между печами
5. **Удобство** - не нужно вручную выбирать каждый раз

---

**URL:** http://localhost:5173  
**Вкладка:** "2. Расчет стоимости" → Селектор печи обновляется автоматически

Теперь система умно выбирает печь для расчета стоимости! 🎯
