# Исправление: Ввод цены камня за кг или за м²

## Дата: 3 февраля 2026

## Проблема

Был создан компонент "Желаемая цена продажи", но пользователю нужен был выбор ввода **цены камня** (не продажи), причем с возможностью ввода как за кг, так и за м².

## Решение

### 1. Удален неправильный компонент

**Удален:** `src/components/PriceInputSelector.tsx` (желаемая цена продажи)

### 2. Создан правильный компонент

**Создан:** `src/components/StoneCostInput.tsx`

**Функционал:**
- Переключатель между двумя режимами:
  - **За 1 кг (mil Rp)** - основной режим
  - **За 1 м² (mil Rp)** - альтернативный режим
- Автоматический пересчет между режимами
- Показ примерного значения в другом формате
- Кнопки "База данных" и "Автоподбор"

### 3. Структура компонента

```typescript
interface StoneCostInputProps {
  onStoneCostChange: (costPerKg: number) => void;
  initialCostPerKg?: number;
  onShowDB: () => void;
  onAutoFind: () => void;
  showDB: boolean;
}
```

### 4. Логика пересчета

**От кг к м²:**
```typescript
const estimatedSqM = costPerKg * 10; // примерный коэффициент
```

**От м² к кг:**
```typescript
const estimatedKg = costPerSqM / 10; // примерный коэффициент
```

**Важно:** Используется примерный коэффициент 10, так как точный пересчет зависит от:
- Толщины камня
- Плотности материала
- Типа изделия

### 5. Внешний вид

```
┌───────────────────────────────┐
│ Цена камня                    │
├───────────────────────────────┤
│ ● За 1 кг (mil Rp)            │
│ ○ За 1 м² (mil Rp)            │
├───────────────────────────────┤
│ Цена за 1 кг (mil Rp)         │
│ [1.095]                       │
│                               │
│ ≈ 10.95 mil Rp за м²          │
├───────────────────────────────┤
│ [База данных]                 │
│ [Автоподбор]                  │
└───────────────────────────────┘
```

### 6. Использование в App.tsx

```tsx
{kilnResults[costKilnType as KilnType] && (
  <StoneCostInput
    onStoneCostChange={setStoneCost}
    initialCostPerKg={stoneCost}
    onShowDB={() => setShowStoneDB(!showStoneDB)}
    onAutoFind={() => {
      const currentResult = kilnResults[costKilnType as KilnType];
      if (currentResult) {
        handleAutoFindStone(currentResult.product);
      }
    }}
    showDB={showStoneDB}
  />
)}
```

## Изменения в файлах

1. ✅ **Удален:** `src/components/PriceInputSelector.tsx`
2. ✅ **Создан:** `src/components/StoneCostInput.tsx`
3. ✅ **Изменен:** `src/App.tsx`
   - Удален импорт `PriceInputSelector`
   - Добавлен импорт `StoneCostInput`
   - Удален state `desiredPricePerSqM`
   - Заменен компонент в JSX

## Результат сборки

✅ **Сборка успешна**
```
CSS: 17.06 kB (3.07 kB gzipped)
JS:  176.00 kB (54.56 kB gzipped)
```

## Как работает

### Режим "За 1 кг":
1. Пользователь вводит: `1.095 mil Rp`
2. Система показывает: `≈ 10.95 mil Rp за м²`
3. Система использует для расчетов: `1.095 mil Rp/кг`

### Режим "За 1 м²":
1. Пользователь вводит: `10.00 mil Rp`
2. Система показывает: `≈ 1.000 mil Rp за кг`
3. Система использует для расчетов: `1.000 mil Rp/кг`

**Важно:** Система всегда работает внутренне с ценой за кг, независимо от того, в каком формате пользователь вводит данные.

## Преимущества

1. ✅ **Гибкость:** Пользователь выбирает удобный формат
2. ✅ **Наглядность:** Показывается пересчет в другой формат
3. ✅ **Единый результат:** Всегда расчет на основе цены за кг
4. ✅ **Простота:** Понятный интерфейс с радио-кнопками
5. ✅ **Интеграция:** Работает с базой данных и автоподбором

---

**URL:** http://localhost:5173  
**Вкладка:** "2. Расчет стоимости" → "Цена камня"

Теперь пользователь может вводить цену камня как за кг, так и за м²! 💎
